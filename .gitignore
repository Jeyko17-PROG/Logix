# Carpetas ajenas a este proyecto que no deben mezclarse aquí (por si se
# vuelven a crear/copiar por accidente en la raíz del repo).
/free-claude-code-backup/

# Cruft común de SO/editor.
.DS_Store
Thumbs.db

# Docker Compose de producción: secretos reales y config generada localmente.
/.env
/docker/nginx/app.conf
