# Configuración de PRODUCCIÓN (build de Vercel).
# El desarrollo local (npm run dev) ignora este archivo y usa el proxy de Vite a localhost:8000.

# URL pública del backend Laravel en Render.
VITE_API_URL=https://logix-backend-wla9.onrender.com

# URL pública para enlaces/QR de reservas. Vacío = usa el origen actual (la URL de Vercel).
VITE_PUBLIC_URL=
